var TinyTester = {

  run: function(tests) {
    var successes = 0;
    var failures = 0;

    for (var testName in tests) {
      var testAction = tests[testName];

      try {
        catch (e) {

        }
      }
    }
  }
};
